# Despliegue en Render (plan gratuito). En Render: New → Blueprint → elige este repositorio.
services:
  - type: web
    name: tenka-ichi
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /salud
    envVars:
      - key: PANEL_CLAVE
        sync: false        # Render te la pedirá al crear el servicio
      - key: GOOGLE_SHEET_ID
        sync: false
      - key: GOOGLE_CREDENTIALS
        sync: false
      # Gachapon: inicio de sesión con Twitch y puntos del canal (app gratis en dev.twitch.tv)
      - key: TWITCH_CLIENT_ID
        sync: false
      - key: TWITCH_CLIENT_SECRET
        sync: false
      - key: SESION_SECRETO
        sync: false
